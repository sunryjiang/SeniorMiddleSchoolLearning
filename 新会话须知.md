# 新会话须知（高中理科全科复习项目）

> 换到新的 Cursor 窗口/会话继续做这个项目时，先让 AI 读这份文件即可无缝接手。
> 建议开场提示词：
> **“这是‘高中理科复习’项目，先读 `新会话须知.md` 和 `README.md`，然后帮我 XXX。”**

---

## 1. 这是什么项目

高中理科全科复习学习工具，**电脑网站 + 安卓 App 共用同一套网页内容**。
首页四大学科：**数学 / 物理 / 化学 / 生物**，点进去按知识点学习（讲解 + 公式 + 表格 + 练习判分）。

架构：**内容数据(JSON) → 静态网页(www) → Capacitor 打包安卓 App → GitHub Actions 云端出 APK**。
（与另一个已完成项目「初中英语提升」是同一套骨架，代码复用而来。）

## 2. 目录结构（都在 `android-app/` 内）

```
android-app/
├─ www/                     ← 网页内容（= 本地网站，也 = App 的界面）
│  ├─ index.html            四科入口首页
│  ├─ subject.html          通用学科页（?subject=math/physics/chemistry/biology）
│  ├─ subject.js            学科引擎：目录 / 讲解 / 公式表格 / 练习判分 / 上一下一 / 返回
│  ├─ style.css             样式（含 .b-formula 公式块、移动端适配、返回/目录键）
│  ├─ tts-bridge.js         朗读桥接（WebView 里用原生 TTS；如需读文本才用得上）
│  ├─ hw-back.js            安卓系统返回键：返回上一界面，最上层才退出
│  ├─ 启动网站.bat           电脑上一键启动本地网站
│  └─ data/                 ★内容数据（增改内容只动这里）
│     └─ math.json  physics.json  chemistry.json  biology.json
├─ capacitor.config.json    appId=com.gaozhong.lksk，App 名=高中理科复习
├─ android/                 Capacitor 生成的安卓工程（已含 TTS + App 两个插件）
├─ .github/workflows/build-apk.yml   云端打包（已配好 Node 22 + JDK 21）
├─ .gitignore  README.md  新会话须知.md
```

## 3. 怎么加/改内容（最常做的事）

编辑 `www/data/<学科>.json`。**三级目录结构**：大类(`category`) → 子类(`subcategory`) → 知识点(`item`)。
公式用 **KaTeX**（LaTeX 语法）；配图用内联 **SVG**（离线、矢量、图文并茂）。结构：

```jsonc
{
  "subject": "数学",
  "categories": ["集合与常用逻辑用语", "函数的概念与性质"],  // 大类，按课本册次顺序（高一→高二→高三，但不显示年级字样）
  "items": [
    {
      "id": "1.1",
      "category": "集合与常用逻辑用语",     // 属于哪个大类
      "subcategory": "集合及其运算",        // 属于哪个子类（可选；同一子类的知识点会归到一起）
      "title": "集合的概念与表示",
      "sections": [
        { "heading": "知识点", "blocks": [   // 建议章节：知识点 / 原理与方法 / 例题详解
          { "kind": "para",    "text": "段落，可含行内公式 $a\\in A$" },
          { "kind": "sub",     "text": "小标题（加粗）" },
          { "kind": "formula", "text": "\\sqrt{ab}\\le \\frac{a+b}{2}" },  // 整块居中公式，写 LaTeX（不要再加 $）
          { "kind": "bullet",  "text": "要点条目，也可含 $x^2$" },
          { "kind": "note",    "text": "易错提示（红色框）" },
          { "kind": "table",   "headers": ["列1","列2"], "rows": [["$\\mathbf{N}$","自然数集"]] },
          { "kind": "svg",     "svg": "<svg viewBox='0 0 300 180' xmlns='http://www.w3.org/2000/svg'>…</svg>", "caption": "图1 说明" },
          { "kind": "image",   "src": "img/xxx.svg", "caption": "图2 说明" }   // 也支持外部图片文件
        ] },
        { "heading": "例题详解", "blocks": [
          { "kind": "eg", "label": "例1", "text": "题干（可含 $公式$）",
            "solution": ["解析第一行", "第二行 $x=2$", "……"] }   // solution 为数组，逐行显示
        ] }
      ],
      "exercises": [
        { "q": "题干，可含 $公式$", "answer": "答案", "tip": "解析（可选）", "level": "中档" }  // level: 基础/中档/较难
      ]
    }
  ]
}
```

**KaTeX 写法要点**（在 JSON 里所有反斜杠要写成 `\\`）：
- 行内公式用 `$...$`，写在 para/bullet/note/eg/表格单元格里。
- `formula` 块内直接写 LaTeX，引擎自动整块居中渲染（不用再包 `$`）。
- 常用：`\\frac{a}{b}` 分式、`\\sqrt{x}` 根号、`x^{2}` 上标、`x_{1}` 下标、`\\le \\ge \\ne \\in \\subseteq \\cup \\cap \\Rightarrow \\forall \\exists`、`\\mathbf{R}` 数集。
- 化学方程式可用 mhchem：`$\\ce{2H2 + O2 -> 2H2O}$`（已加载 mhchem 扩展）。

规则：
- 填空判分**忽略空格与大小写**；一题多个可接受答案用 `/` 分隔，如 `"氧气/O₂/氧"`。区间答案如 `(-2,2)`、集合答案如 `{1,2}` 直接写。
- 大类按课本册次顺序排（数学=人教A版必修一/二→选择性必修一/二/三；界面不显示"高一/高二/高三"）。
- 新加学科：在 `data/` 放 `xxx.json`，并在 `index.html` 加一张卡片链接 `subject.html?subject=xxx`。
- 依据：**人教版 + 湖北省新高考（3+1+2）** 要求。

离线库位置：`www/vendor/katex/`（katex.min.css/js、contrib/auto-render.min.js、contrib/mhchem.min.js、fonts/）。

## 4. 电脑上预览网站

进入 `www` 双击 **`启动网站.bat`**；或命令行：

```powershell
cd www
python -m http.server 8124
```

浏览器打开 http://localhost:8124/index.html （不要直接双击 index.html，会因浏览器限制加载失败）。

## 5. 打包成安卓 APK（GitHub Actions 云端，本地无需装安卓工具）

1. 给本项目**新建一个独立的 GitHub 仓库**（不要复用英语项目的仓库）。
2. 把 `android-app` 目录推上去（`git init` → 提交 → push）。
3. 仓库 `Actions` 页会自动运行「构建安卓 APK」；成功后在该运行页面底部 `Artifacts` 下载
   **`gaozhong-lksk-apk`**，解压得 `app-debug.apk`，传手机安装。
4. 以后每次改内容再 push，会自动出新 APK。

## 6. 已知要点 / 避坑

- CI 已用 **Node 22**（Capacitor 8 要求 ≥22，早期用 20 会失败，这里已避开）+ **JDK 21**。
- 若加了英文/文本朗读，手机要自然发音需装 **Google 文字转语音引擎**并设为默认、下载英文语音。
- 公式目前是**纯文本**（如 `y = ax² + bx + c`）。若要漂亮排版（分式/根号/上下标），可引入
  **MathJax 或 KaTeX**（把库文件放进 www，本地离线加载），需要时让 AI 帮忙接入。

## 7. 参考：另一个同架构项目

「初中英语提升」在 `C:\CUDA\FastAPI\Test\android-app`（GitHub: sunryjiang/English4MidSchool）。
想参考它的做法（如背单词卡片、分级阅读、音标生成、发音口音切换等），可把该目录
Add Folder to Workspace，或直接把路径告诉 AI。

## 8. 当前进度

- 引擎已升级（v2）：三级目录（大类/子类/知识点、可折叠）、KaTeX 离线公式、SVG/图片配图、例题解析多行、练习难度标签。本地已验证资源与数据均 200/合法。
- **数学：已全部完成！**（人教A版，湖北新高考全部考查）共 18 大类、60 个知识点、19 张配图、249 道分层练习。册次顺序：
  - 必修一：集合与常用逻辑用语 / 一元二次函数、方程和不等式 / 函数的概念与性质 / 指数函数与对数函数 / 三角函数
  - 必修二：平面向量及其应用 / 复数 / 立体几何初步 / 统计 / 概率
  - 选择性必修一：空间向量与立体几何 / 直线和圆的方程 / 圆锥曲线的方程
  - 选择性必修二：数列 / 一元函数的导数及其应用
  - 选择性必修三：计数原理 / 随机变量及其分布 / 成对数据的统计分析
  - 每个知识点含 知识点 / 原理与方法 / 例题详解（含配图）/ 巩固练习（基础·中档·较难）。
- **物理：已全部完成！**（人教版，湖北新高考物理选考全部）共 18 大类、40 个知识点、8 张配图、160 道分层练习。顺序：
  - 力学：运动的描述 / 匀变速直线运动 / 相互作用力 / 牛顿运动定律 / 抛体运动 / 圆周运动 / 万有引力与宇宙航行 / 机械能守恒定律
  - 电磁学：静电场 / 恒定电流 / 磁场 / 电磁感应 / 交变电流
  - 其他：动量守恒定律 / 机械振动与机械波 / 光 / 热学 / 近代物理
  - 配图含 v-t图、平抛、圆周向心力、力的合成、电场线、电路图、安培力、光的折射。
- **化学：已全部完成！** 15 大类、28 知识点、105 练习。必修（物质分类/离子反应/氧化还原/钠/氯/铁/元素周期律/反应与能量/简单有机物）+ 选修（反应热与盖斯定律/速率与平衡/水溶液离子平衡/电化学/物质结构与性质/有机化学基础）。方程式用 mhchem，含原子结构、原电池配图。
- **生物：已全部完成！** 20 大类、27 知识点、104 练习。必修（分子与细胞 6 章 + 遗传与进化 6 章）+ 选修（内环境/神经/体液/免疫/植物激素/种群群落/生态系统/生物技术与工程）。含细胞结构、DNA 双螺旋、反射弧配图。

## 英语（多模块，人教版·湖北新高考）

英语是**多模块子应用**：总首页 `index.html` →「英语」卡片 → `english.html`（英语首页，7 个模块卡片）。返回逻辑：`subject.js` 支持 `?back=english.html`。

- **语法精讲**：`subject.html?subject=en-grammar` ← `data/en-grammar.json`（14 大类 24 知识点，含 8 个「高中重点」；每知识点≥2 例句，逐条列举的表格加了"例句"列）。
- **背单词**：`vocab.html` + `vocab.js` ← `data/vocab.json`（units→words{word,pos,ipa,cn,eg}，卡片/点读/口音）。**目前 40 词，需按话题分批扩到高考 3500 词**（这是主要待办）。
- **分级阅读**：`reading.html` + `reading.js` ← `data/reading.json`（items{day,week,group,level,title,theme,sentences[{en,cn}],words[],questions[{q,a,tip}]}；点读+朗读全文+理解填空）。已 5 篇。
- **听力**：`listening.html`（复用 `reading.js`，靠 `window.READING_DATA_FILE` 指向 `data/listening.json`）。已 3 段。
- **写作**：`subject.html?subject=en-writing` ← `data/en-writing.json`（应用文/读后续写/提分句型，含范文）。
- **题型专练**：`subject.html?subject=en-exam` ← `data/en-exam.json`（完形/语法填空/七选五）。
- **高频短语**：`subject.html?subject=en-phrases` ← `data/en-phrases.json`。
- 朗读：`vocab.js`/`reading.js` 用 Web Speech / 原生 TTS（`tts-bridge.js`），跟随 `localStorage.ttsAccent`（英/美）。
- 待办：① 词汇扩到 3500（分批）；② 阅读/听力/各模块继续增量补篇目与练习。

## 语文（新高考要求）

- 入口：总首页「语文」卡片 → `subject.html?subject=chinese` ← `data/chinese.json`（复用理科引擎）。
- 已建 7 大类、23 知识点、71 练习：现代文阅读（信息类/小说/散文）、文言文阅读（实词/虚词/句式/翻译）、古代诗歌鉴赏（形象意境/表达技巧/情感题材）、名篇名句默写（劝学师说/苏轼/唐诗/其他，理解性默写填空）、语言文字运用（成语/病句/连贯得体/修辞）、写作（审题立意/议论文结构/素材语言）、文学文化常识（古代文化/文学常识）。
- 待办：可继续增补默写篇目、文言实词虚词条目、阅读方法与真题化练习。

## 全科完成情况（理科四科 + 语文 已建；英语 7 模块已成，词汇持续扩充中）

| 学科 | 大类 | 知识点 | 配图 | 练习 |
|---|---|---|---|---|
| 数学 | 18 | 60 | 19 | 249 |
| 物理 | 18 | 40 | 8 | 160 |
| 化学 | 15 | 28 | 2 | 105 |
| 生物 | 20 | 27 | 3 | 104 |
| 合计 | 71 | 155 | 32 | 618 |

均为人教版、湖北新高考要求，统一采用「知识点 / 原理与方法 / 例题详解（含配图）/ 巩固练习（基础·中档·较难）」结构，公式 KaTeX、化学式 mhchem、配图内联 SVG。后续可继续加深各章例题数量与配图。
- **尚未** `git init`、**尚未**建 GitHub 仓库、**尚未**打过 APK —— 留待按第 5 节进行。
